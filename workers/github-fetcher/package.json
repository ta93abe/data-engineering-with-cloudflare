{
  "name": "github-fetcher",
  "version": "1.0.0",
  "description": "Cloudflare Worker to fetch GitHub data from Queue and save to R2",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "apache-arrow": "^18.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.100.0"
  }
}
