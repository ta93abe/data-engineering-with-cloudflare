models:
  - name: stg_travelers
    description: Traveler data with basic cleaning applied, one row per traveler.
    columns:
      - name: traveler_id
        description: The unique key for each traveler.
        data_tests:
          - not_null
          - unique
