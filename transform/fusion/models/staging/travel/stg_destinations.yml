models:
  - name: stg_destinations
    description: Destination data with basic cleaning applied, one row per destination.
    columns:
      - name: destination_id
        description: The unique key for each destination.
        data_tests:
          - not_null
          - unique
