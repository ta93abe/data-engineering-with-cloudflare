models:
  - name: dim_traveler
    description: Traveler dimension, one row per traveler.
    columns:
      - name: traveler_id
        description: Primary key for each traveler.
        data_tests:
          - not_null
          - unique
      - name: email
        description: "Email address of the traveler. PII: should be hashed or masked before production use."
