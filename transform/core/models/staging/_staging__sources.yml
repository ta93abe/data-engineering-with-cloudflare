version: 2

sources:
  - name: raw
    description: "Raw data from R2 bucket"
    # schema: "s3://{{ env_var('R2_BUCKET_NAME') }}/raw"

    # Freshness checks
    # freshness:
    #   warn_after: {count: 12, period: hour}
    #   error_after: {count: 24, period: hour}
    # loaded_at_field: _loaded_at

    tables:
      - name: sample_data
        description: "Sample data for development"
        # external:
        #   location: "sample/**/*.parquet"
        #   file_format: parquet
